name: Laravel CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  # Job 1: Test the code
  laravel-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2' # Ensure this matches your server PHP version
      - name: Copy .env
        run: php -r "file_exists('.env') || copy('.env.example', '.env');"
      - name: Install Dependencies
        run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
      - name: Generate key
        run: php artisan key:generate
      - name: Directory Permissions
        run: chmod -R 777 storage bootstrap/cache
      - name: Create Database
        run: |
          mkdir -p database
          touch database/database.sqlite
      - name: Execute tests
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite
        run: php artisan test

  # Job 2: Deploy to Server
  deploy:
    needs: laravel-tests
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
        run: |
          mkdir -p ~/.ssh
          # Write the secret to the file safely
          echo "$DEPLOY_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          # Removed ssh-keyscan because StrictHostKeyChecking=no handles it!

      - name: Deploy to aaPanel
        run: |
         ssh -o StrictHostKeyChecking=no -p 22 -i ~/.ssh/id_rsa root@212.47.68.230 "
          set -e 
          
          mkdir -p /www/wwwroot/clovercompany/bhasbni
          cd /www/wwwroot/clovercompany/bhasbni
          
          echo '🔄 Forcing Git to download latest files...'
          git config --global --add safe.directory /www/wwwroot/clovercompany/bhasbni
          
          git init
          git remote remove origin 2>/dev/null || true
          git remote add origin https://${{ secrets.GITHUB_TOKEN }}@github.com/MegoABKM/Hasbni_Backend.git
          
          git fetch origin
          git reset --hard origin/main
          
          echo '📦 Installing Composer...'
          export COMPOSER_ALLOW_SUPERUSER=1
          
          # Using specific PHP version 8.2 (Change '82' to '83' if you are using PHP 8.3)
          /www/server/php/82/bin/php /usr/bin/composer install --no-dev --optimize-autoloader --ignore-platform-req=ext-fileinfo
      
          if [ ! -f .env ]; then
             echo '⚙️ Setting up .env...'
             cp .env.example .env
             /www/server/php/82/bin/php artisan key:generate
          fi

          echo '🗄️ Running Laravel commands...'
          /www/server/php/82/bin/php artisan migrate --force
          /www/server/php/82/bin/php artisan config:clear
          /www/server/php/82/bin/php artisan cache:clear
          /www/server/php/82/bin/php artisan route:clear
          /www/server/php/82/bin/php artisan view:clear
      
          echo '🔒 Fixing Permissions for aaPanel...'
          chown -R www:www /www/wwwroot/clovercompany/bhasbni
          chmod -R 755 /www/wwwroot/clovercompany/bhasbni
          chmod -R 775 /www/wwwroot/clovercompany/bhasbni/storage
          chmod -R 775 /www/wwwroot/clovercompany/bhasbni/bootstrap/cache
      
          echo '✅ Deployed successfully!'
         "
